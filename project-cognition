一、文档目的
本文档用于重新定义系统中的记忆管理机制，使其不再被表述为若干具体论文方法的拼接，而是作为一个统一的、可扩展的、可治理的记忆基础设施来支撑多智能体系统中的长期状态维护、共享协作与推理增强。
在这一新设计中，系统的重点不是集成某两个现成项目本身，而是抽取其中更底层、可泛化的机制，纳入统一的 memory substrate。具体而言，系统需要同时解决两类核心问题。
第一类问题是记忆与多智能体系统之间的关系管理，也就是多用户、多 agent、多资源、多权限条件下，记忆如何被生产、归属、共享、可见、投影、追踪与审计。
第二类问题是记忆实体本身如何被管理，也就是一条记忆如何被抽取、压缩、合并、强化、遗忘、摘要化、画像化或图结构化。这一层不应被绑定到某个单一算法，而应被设计成可插拔的记忆实体管理算法接口。MemoryBank 只是这一层可接入的一个算法实例，未来还可以继续接入其他算法，或使用系统自研算法。
因此，本系统的总体目标是构建一套：
- 以对象系统为核心的记忆表示层
- 以事件流为主干的状态演化层
- 以关系治理为核心的共享控制层
- 以算法插件为核心的记忆实体管理层
- 以统一视图为出口的检索与推理支撑层

---
二、总体设计原则
2.1 记忆系统不等于向量库
系统中的记忆不应被理解为简单的文本片段或 embedding chunk。对于 agent-native database 而言，记忆应被视为具有归属、来源、作用域、访问关系、时间状态与版本信息的对象。
因此，系统中的记忆管理不只是向量存储与 top-k 检索，而是一个覆盖以下问题的完整基础设施：
- 记忆如何形成
- 记忆归属于谁
- 记忆和哪些 agent 相关
- 记忆和哪些 resource 或工具轨迹相关
- 记忆何时可见
- 记忆是否能跨角色共享
- 记忆在时间中如何强化、衰减、总结或迁移
- 记忆最终以什么形式被上层推理消费
2.2 记忆关系治理与记忆实体管理必须解耦
系统必须明确区分两个层次。
第一层是记忆关系治理层。它回答的是，记忆和用户、agent、resource、session、workspace、tenant、policy 之间是什么关系，以及这些关系如何决定记忆的可见性、共享性与审计性。
第二层是记忆实体管理层。它回答的是，一条记忆对象内部怎样被更新、压缩、聚合、遗忘或强化。
这两个层次不能混在一起。因为前者属于系统机制，后者属于算法机制。系统必须保证即使更换记忆实体管理算法，记忆关系治理层仍然成立。
2.3 记忆管理算法必须插件化
记忆实体管理算法不应被写死在主系统里，而应被抽象为标准接口。系统可以接入不同算法，例如：
- 强化与遗忘型算法
- 摘要压缩型算法
- 图结构记忆算法
- 关联链接型算法
- 多模态分类型算法
- 自研面向 MAS 的记忆调度算法
MemoryBank 可以作为其中一种可接入算法，但系统设计本身不应围绕某一单独算法展开。
2.4 上层 agent 不直接操作底层记忆存储
无论是写入还是读取，agent 都不应直接操作 raw memory store。正确的系统路径应是：
interaction -> event backbone -> memory object materialization -> policy-controlled view -> query/planner/reasoner（一个例子需要根据实际的系统设计来修改）
也就是说，记忆首先是对象，其次才是检索材料；首先受治理，再用于推理。

---
三、统一记忆管理架构的总体分层
建议把系统中的记忆管理机制拆成六个基础层（文档是单独设计的，但是插入到系统中可以适当的设置成worker，因为有些layer在整个系统中已经预留好位置）。
3.1 Relation and Governance Layer
这一层管理记忆和 MAS 中各类主体之间的关系，是整个系统的底层治理骨架。
它负责回答：
- 这个记忆属于哪个用户、哪个 agent、哪个会话或哪个共享空间
- 哪些 agent 可以看到它
- 哪些用户可以间接通过 agent 使用它
- 记忆形成时访问了哪些资源
- 这些资源访问关系会如何影响后续可见性
- 当前请求在此时此刻应得到什么记忆视图
这层不关心记忆是否由某种算法强化或遗忘，只关心关系、边界、权限与流通。
3.2 Event Backbone Layer
这一层是记忆状态演化的主干。系统中所有与记忆有关的变化都应首先表现为事件，而不是对某张 memory 表直接 upsert。
它负责：
- 记录 interaction 与 tool/resource access
- 记录记忆创建、更新、召回、共享、归档与删除请求
- 为 provenance 提供来源
- 为审计和回放提供日志基础
- 为后续异步物化提供统一输入
3.3 Canonical Memory Object Layer
这一层负责把原始事件与原始内容物化为统一记忆对象。系统中的记忆不再只是字符串，而是带有完整 metadata、归属关系、来源链路和生命周期状态的对象。
3.4 Memory Entity Management Layer
这一层是算法插件层。它负责对记忆实体本身做进一步管理，例如抽取、合并、摘要、强化、遗忘、画像、图建模、层级压缩等。
这一层必须通过统一接口挂接，允许接入 MemoryBank 或其他算法。
3.5 Retrieval and View Construction Layer
这一层负责把底层记忆对象与算法管理结果构造成可供当前请求使用的视图。它的输出不是原始库条目，而是 policy-conditioned、scope-aware、algorithm-processed 的 MemoryView。
3.6 Query Consumption Layer
这一层对应 Query Worker、Planner、Reasoner、Tool Executor 等上层 agent 运行单元。它们只能消费 MemoryView，而不能绕过系统直接读取底层对象或索引。

---
四、记忆与 MAS 关系管理的底层机制设计
这一部分是整个系统最核心的底层设计。它不依赖某个具体项目，而是抽象出记忆系统在多智能体环境中的关系治理机制。
4.1 基本设计目标
系统必须把记忆视为 MAS 运行过程中形成的一类受治理对象。该对象既不是完全私有的，也不是天然共享的，而是处于不同 scope、role、policy 和 provenance 条件之下。
因此，系统必须原生支持：
- 记忆归属
- 记忆关联
- 记忆可见性
- 记忆共享层级
- 记忆投影与脱敏
- 记忆追踪与审计
- 记忆时态有效性
- 记忆与 agent/resource 的绑定关系
4.2 关系对象模型
建议在系统中显式建模如下对象：
- User
- Agent
- Resource
- Workspace
- Session
- MemoryObject
- MemoryRelation
- PolicySnapshot
- AuditRecord
其中，MemoryRelation 是关键对象，它不只是普通 join 表，而是系统对记忆关系语义的正式表达。
4.3 MemoryRelation 的语义类型
建议 MemoryRelation 至少支持以下类型：
- owned_by
- created_by
- observed_by
- accessible_by
- derived_from
- grounded_on_resource
- shared_with
- scoped_to
- projected_from
- used_in_response
- updated_by_algorithm
这样做的意义在于，记忆与 MAS 的关系不再隐藏在代码逻辑里，而是成为系统层可查询、可检索、可审计的对象网络。
4.4 动态访问关系图
在多智能体系统中，谁可以访问什么，不应被写死为静态字段，而应表现为动态关系图。
建议维护至少两类时变关系：
- 主体到 agent 的调用关系
- agent 到 resource 的访问关系
但系统不必把这两类关系局限为某个具体论文中的形式化符号，而应在工程上抽象为 AccessGraphSnapshot。
AccessGraphSnapshot 用于回答某一时刻：
- 哪个用户可以调用哪些 agent
- 哪些 agent 可读取哪些资源
- 当前 workspace 或 session 下记忆流通边界是什么
- 当前请求的记忆可见边界是什么
4.5 记忆作用域模型
系统中的记忆必须有 scope，而不是一律进入同一个 memory namespace。
建议至少支持以下 scope：
- private_user
- private_agent
- session_local
- workspace_shared
- team_shared
- global_shared
- restricted_shared
scope 决定的是流通边界，不等于最终可见性。最终可见性仍需结合 policy、provenance 和当前 access graph 决定。
4.6 共享不是复制，而是受控投影
在 MAS 系统中，记忆共享不应简单理解为把一条记录拷贝到共享库。更底层、更合理的方式是：
- 原始记忆对象保留原始归属和 provenance
- 系统根据请求上下文生成共享投影
- 不同请求可以看到同一记忆对象的不同投影版本
因此，系统需要显式支持：
- raw object
- redacted projection
- summary projection
- abstracted projection
- metadata-only projection
这意味着记忆共享的基本单元不是复制，而是 projection。
4.7 provenance 必须是第一类公民
任何可流通记忆都必须带 provenance，而 provenance 不能只是一个附属注释字段。它必须是系统级约束。
建议每条记忆对象都保留：
- 创建来源事件
- 创建时参与的 user / agent
- 创建时访问的 resource
- 派生链条
- 之后被哪些算法更新过
- 之后被哪些请求使用过
这样系统才能支持：
- 记忆解释
- 审计回溯
- 共享合法性检查
- 错误传播追踪
- rollback 与重构
4.8 审计机制
所有共享、读取、投影、算法更新、降权、归档等动作都应形成审计记录。
建议 AuditRecord 至少支持：
- record_id
- target_memory_id
- operation_type
- actor_type
- actor_id
- policy_snapshot_id
- decision
- reason_code
- timestamp
- downstream_request_id
审计层的意义不是事后补日志，而是把系统中的记忆治理过程显式化。

---
五、统一记忆对象模型设计
5.1 核心对象定义
建议系统中的基础记忆对象统一定义为 MemoryObject，而不是直接区分为某种算法专属对象。
MemoryObject 应至少包含以下字段：
- memory_id
- memory_type
- content_raw
- content_normalized
- summary
- embedding
- scope
- owner_id
- owner_type
- created_at
- updated_at
- status
- provenance_ref
- policy_tags
- lifecycle_state
- algorithm_state_ref
- relation_refs
- version
这里的 algorithm_state_ref 非常关键，它允许不同记忆管理算法把自己的内部状态外挂到统一对象上，而不破坏主系统的 canonical schema。
5.2 对象子类型
在统一对象层上，可以进一步定义若干常见子类型：
- ConversationMemory
- EventMemory
- SummaryMemory
- ProfileMemory
- SharedMemoryProjection
- ToolTraceMemory
- ResourceGroundedMemory
- StateMemory
- PolicyMemory
这些子类型属于系统对象分类，而不等于具体算法分类。
5.3 生命周期状态
记忆对象应具有完整生命周期，而不是仅有是否存在。
建议支持：
- active
- compressed
- decayed
- archived
- quarantined
- hidden
- deleted_logically
这让系统可以支持算法层的强化与遗忘，也可以支持治理层的隔离与隐藏。

---
六、记忆实体管理算法层的抽象设计
这一层是用来接 MemoryBank 及其他算法的地方。其关键不是描述某个算法，而是定义算法接入主系统的方式。
6.1 设计目标
记忆实体管理算法层负责对 MemoryObject 做内容级和状态级管理，而不改变主系统的关系治理骨架。
它可以执行的工作包括：
- 从 interaction 中抽取高价值记忆
- 合并重复记忆
- 建立层级摘要
- 维护用户或 agent 的 profile object
- 对记忆分配强度、权重或保留度
- 根据使用情况进行强化与遗忘
- 构建图关系、索引链接或多模态分类结果
- 输出算法特定的 retrieval hint 或 ranking signal
6.2 算法插件接口
建议定义统一插件接口：
MemoryManagementAlgorithm
至少包括以下方法：
- ingest(memory_objects, context) -> updates
- update(memory_objects, signals) -> updates
- recall(query, candidate_memories, context) -> scored_candidates
- compress(memory_objects, context) -> derived_memories
- decay(memory_objects, time_signal) -> state_updates
- summarize(memory_objects, context) -> summary_objects
- export_state(memory_id) -> algorithm_state
- load_state(memory_id, algorithm_state)
这样，系统就可以在不改变主架构的前提下，接入不同算法。
6.3 算法状态外挂机制
不同算法通常会维护自己的内部状态，例如：
- 强度值
- 遗忘参数
- profile 累积状态
- 图节点链接
- 多模态特征缓存
- 主题分布或聚类编号
这些状态不应硬塞进主 schema，而应通过 algorithm_state_ref 挂接到独立算法状态存储区。
这样做有三个好处：
- 主对象模型保持稳定
- 新算法接入成本低
- 多算法可并行作用于同一对象
6.4 算法执行模式
算法层建议支持三种执行模式：
- 写入时增量执行
- 召回时在线重排序或重打分
- 后台异步维护执行
这样，一个算法既可以在写入时抽取记忆，也可以在检索时重排，还可以在后台做压缩、遗忘、合并和摘要更新。

---
七、以 MemoryBank 为例的算法接入方式
MemoryBank 在这里不应被表述为系统的一个独立平行子系统，而应被视为“记忆实体管理算法插件”的一个例子。
7.1 可映射的能力
MemoryBank 适合映射到以下几个算法能力：
- 时间顺序对话记忆管理
- 分层摘要构建
- 用户画像或 profile object 构建
- 基于召回行为的强化机制
- 基于时间的遗忘或衰减机制
7.2 接入方式
当 MemoryBank 作为算法插件接入时，系统中的 MemoryObject 仍是统一对象。MemoryBank 只负责给这些对象附加内部状态和派生对象。
例如：
- 原始对话被物化为 ConversationMemory
- MemoryBank 生成 SummaryMemory
- MemoryBank 生成 ProfileMemory
- MemoryBank 更新对象的 strength、retention 等算法状态
- 检索时，MemoryBank 提供候选重打分或补充摘要对象
7.3 建议的算法状态字段
对于采用类似 MemoryBank 的算法，可以在算法状态区保存：
- memory_strength
- last_recalled_at
- recall_count
- retention_score
- portrait_state
- summary_link_refs
7.4 工程上的正确表述
系统设计文档中应避免写成“本系统由某算法和某共享机制组成”。更合理的表述应是：
本系统在记忆实体管理层支持接入强化与遗忘型长期记忆算法。MemoryBank 是当前可接入的一个算法实例，用于管理对话记忆、摘要记忆和 profile 记忆的生成与动态更新。
这样后续更换或新增算法时，主系统文档无需推翻重写。

---
八、数据流与物化链路设计
8.1 写入主链路
统一的写入主链路应为：
interaction/request -> event backbone -> canonical memory materialization -> relation binding -> algorithm plugin processing -> index/update/view refresh
其中每一步的职责如下。
Interaction / Request
用户请求、agent 响应、tool 调用、resource access 等原始行为进入系统。
Event Backbone
这些行为先写入事件日志，形成可追溯输入。
Canonical Memory Materialization
系统根据事件将原始内容物化为 MemoryObject。
Relation Binding
系统为新对象绑定 owner、scope、workspace、resource grounding、session relation、agent relation 与 provenance。
Algorithm Plugin Processing
记忆管理算法对对象进行抽取、摘要、强化、遗忘、画像或图化更新。
Index / Update / View Refresh
系统更新索引、对象状态与可用视图。
8.2 查询主链路
统一的查询主链路应为：
request -> access graph resolve -> candidate retrieval -> policy filter -> projection -> algorithm-aware rerank -> memory view construction -> query worker
这意味着系统在查询时先考虑关系边界，再考虑算法处理，而不是先把所有记忆都拉出来再打分。

---
九、检索与视图构造层设计
9.1 候选检索不直接面向全文
系统中的检索应分成三步：
- 基于 scope 和 relation 的候选缩减
- 基于内容表示的 dense/sparse retrieval
- 基于算法状态和治理约束的 rerank 与 projection
9.2 MemoryView 作为统一出口
建议定义统一对象 MemoryView，作为上层 agent 可消费的唯一出口。
MemoryView 至少应包含：
- request_id
- requester_id
- agent_id
- resolved_scope
- visible_memory_refs
- projected_memory_payloads
- policy_notes
- provenance_notes
- algorithm_notes
- construction_trace
这里的 algorithm_notes 可以记录某个插件算法给出的加权、摘要、强度或排序信号。
9.3 视图构造的核心逻辑
系统应按如下顺序构造 MemoryView：
1. 根据 access graph 和 scope 找到合法记忆边界
2. 基于 relation graph 缩小候选集合
3. 在候选集合上做 dense/sparse retrieval
4. 调用算法插件进行 recall-aware scoring 或补充摘要对象
5. 对结果做 policy projection 和 redaction
6. 输出最终 MemoryView

---
